'use client'

import { useMemo, useState } from 'react'
import { ArrowRight, Check, ChevronDown, ChevronUp, MapPin, MessageCircle, Minus, Plus, ShoppingBasket, Sprout, X } from 'lucide-react'
import { DISPLAY_PHONE, formatPrice, products, WHATSAPP_NUMBER, type Product, type ProductCategory } from '@/lib/products'

const heroImage = '/images/seedlings-hero.png'
const categoryCopy: Record<ProductCategory, { eyebrow: string; title: string; description: string }> = {
  plants: { eyebrow: 'Vegetable seedlings', title: 'Plants for your next harvest', description: 'Healthy vegetable seedlings for kitchen gardens, market gardens, and productive fields.' },
  trees: { eyebrow: 'Tree seedlings', title: 'Trees for stronger futures', description: 'Build shade, fruit, shelter, and a greener Malawi with trees raised for a strong start.' },
}
type Cart = Record<string, number>

export function AgroFuturisticsStore() {
  const [cart, setCart] = useState<Cart>({})
  const [selected, setSelected] = useState<Product | null>(null)
  const [showCart, setShowCart] = useState(false)
  const [submitted, setSubmitted] = useState(false)
  const [form, setForm] = useState({ name: '', phone: '', method: 'Pickup from Bunda' })
  const cartItems = products.filter((product) => cart[product.id])
  const itemCount = Object.values(cart).reduce((total, quantity) => total + quantity, 0)
  const total = cartItems.reduce((sum, item) => sum + item.price * cart[item.id], 0)

  const updateCart = (id: string, delta: number) => setCart((current) => {
    const quantity = Math.max(0, (current[id] ?? 0) + delta)
    const next = { ...current }
    if (quantity === 0) delete next[id]
    else next[id] = quantity
    return next
  })

  const whatsappUrl = useMemo(() => {
    const lines = cartItems.map((item) => `• ${item.name} (${item.englishName}) x${cart[item.id]} — ${formatPrice(item.price * cart[item.id])}`)
    const message = `Moni AgroFuturistics!%0A%0AI would like to order:%0A${encodeURIComponent(lines.join('\n'))}%0A%0ATotal: ${encodeURIComponent(formatPrice(total))}%0AName: ${encodeURIComponent(form.name)}%0APhone: ${encodeURIComponent(form.phone)}%0AOption: ${encodeURIComponent(form.method)}`
    return `https://wa.me/${WHATSAPP_NUMBER}?text=${message}`
  }, [cartItems, cart, total, form])

  const handleOrder = () => {
    if (!form.name.trim() || !form.phone.trim() || !cartItems.length) return
    setSubmitted(true)
    window.open(whatsappUrl, '_blank', 'noopener,noreferrer')
  }

  return <div className="min-h-screen bg-background text-foreground">
    <header className="sticky top-0 z-40 border-b border-border/70 bg-background/95 backdrop-blur">
      <div className="mx-auto flex max-w-7xl items-center justify-between px-5 py-4 lg:px-8">
        <a href="#top" className="flex items-center gap-3" aria-label="AgroFuturistics home">
          <span className="grid size-10 place-items-center rounded-full bg-primary text-primary-foreground shadow-sm"><Sprout size={21} /></span>
          <span><span className="block font-serif text-lg font-bold tracking-tight text-primary">AgroFuturistics</span><span className="hidden text-[10px] font-semibold uppercase tracking-[0.18em] text-muted-foreground sm:block">Planting today. Harvesting tomorrow.</span></span>
        </a>
        <nav className="hidden items-center gap-7 text-sm font-semibold text-muted-foreground md:flex"><a href="#catalogue" className="transition-colors hover:text-primary">Catalogue</a><a href="#about" className="transition-colors hover:text-primary">About us</a><a href="#contact" className="transition-colors hover:text-primary">Contact</a></nav>
        <button onClick={() => setShowCart(true)} className="relative flex items-center gap-2 rounded-full bg-primary px-4 py-2.5 text-sm font-bold text-primary-foreground transition-transform hover:scale-[1.02]" aria-label={`Open cart with ${itemCount} items`}><ShoppingBasket size={17} /><span className="hidden sm:inline">Your order</span>{itemCount > 0 && <span className="grid size-5 place-items-center rounded-full bg-accent text-xs text-accent-foreground">{itemCount}</span>}</button>
      </div>
    </header>

    <main id="top">
      <section className="relative isolate overflow-hidden bg-primary text-primary-foreground">
        <img src={heroImage} alt="Young seedlings growing in a field" className="absolute inset-0 h-full w-full object-cover opacity-30" />
        <div className="relative mx-auto grid min-h-[570px] max-w-7xl items-center px-5 py-20 lg:grid-cols-[1.1fr_0.9fr] lg:px-8">
          <div className="max-w-2xl"><p className="mb-5 text-sm font-bold uppercase tracking-[0.22em] text-accent">Bunda, Lilongwe · Malawi</p><h1 className="font-serif text-5xl font-bold leading-[1.03] tracking-tight text-balance sm:text-7xl">Quality seedlings.<br /><span className="text-accent">Stronger futures.</span></h1><p className="mt-7 max-w-lg text-lg leading-8 text-primary-foreground/80">Planting today, harvesting tomorrow. Healthy, ready-to-grow seedlings for farmers, families, and gardens across Malawi.</p><div className="mt-9 flex flex-wrap gap-3"><a href="#catalogue" className="inline-flex items-center gap-2 rounded-full bg-accent px-6 py-3.5 font-bold text-accent-foreground transition hover:brightness-105">Browse seedlings <ArrowRight size={17} /></a><a href={`https://wa.me/${WHATSAPP_NUMBER}`} target="_blank" rel="noreferrer" className="inline-flex items-center gap-2 rounded-full border border-primary-foreground/30 px-6 py-3.5 font-bold text-primary-foreground transition hover:bg-primary-foreground/10"><MessageCircle size={17} /> Chat on WhatsApp</a></div></div>
          <div className="mt-12 hidden justify-end lg:flex"><div className="max-w-xs rounded-[2rem] border border-primary-foreground/20 bg-primary-foreground/10 p-7 backdrop-blur-sm"><Sprout className="mb-8 text-accent" size={36} /><p className="font-serif text-2xl font-bold leading-tight">“Mbande zabwino zilipo!”</p><p className="mt-3 text-sm leading-6 text-primary-foreground/70">Good seedlings are here. Let&apos;s grow something meaningful together.</p></div></div>
        </div>
      </section>

      <section className="border-b border-border bg-card"><div className="mx-auto grid max-w-7xl gap-6 px-5 py-7 sm:grid-cols-3 lg:px-8"><div className="flex gap-3"><Check className="mt-0.5 text-primary" size={20} /><div><p className="font-bold">Healthy by design</p><p className="mt-1 text-sm text-muted-foreground">Carefully nurtured for a confident start.</p></div></div><div className="flex gap-3"><Check className="mt-0.5 text-primary" size={20} /><div><p className="font-bold">Ready when you are</p><p className="mt-1 text-sm text-muted-foreground">Yakonzeka kubzalidwa — ready to plant.</p></div></div><div className="flex gap-3"><Check className="mt-0.5 text-primary" size={20} /><div><p className="font-bold">Local &amp; trusted</p><p className="mt-1 text-sm text-muted-foreground">Serving Bunda and Lilongwe with care.</p></div></div></div></section>

      <section id="catalogue" className="mx-auto max-w-7xl px-5 py-20 lg:px-8"><div className="mb-12 flex flex-wrap items-end justify-between gap-5"><div><p className="mb-3 text-sm font-bold uppercase tracking-[0.2em] text-primary">Our catalogue</p><h2 className="font-serif text-4xl font-bold tracking-tight sm:text-5xl">Choose what you&apos;ll grow</h2><p className="mt-3 max-w-xl leading-7 text-muted-foreground">Plants for today&apos;s harvest, trees for tomorrow&apos;s future. Add original product photos in one place: <span className="font-mono text-sm text-primary">lib/products.ts</span>.</p></div><span className="rounded-full bg-secondary px-4 py-2 text-sm font-semibold text-secondary-foreground">{products.length} products</span></div>{(['plants', 'trees'] as ProductCategory[]).map((category) => { const copy = categoryCopy[category]; const categoryProducts = products.filter((product) => product.category === category); return <div key={category} className="mb-16 last:mb-0"><div className="mb-6 max-w-2xl"><p className="mb-2 text-sm font-bold uppercase tracking-[0.2em] text-primary">{copy.eyebrow}</p><h3 className="font-serif text-3xl font-bold tracking-tight sm:text-4xl">{copy.title}</h3><p className="mt-2 leading-7 text-muted-foreground">{copy.description}</p></div><div className="grid gap-5 sm:grid-cols-2 lg:grid-cols-3">{categoryProducts.map((product) => <ProductCard key={product.id} product={product} quantity={cart[product.id] ?? 0} onAdd={() => updateCart(product.id, 1)} onMore={() => setSelected(product)} />)}</div></div>})}</section>

      <section id="about" className="bg-secondary"><div className="mx-auto grid max-w-7xl gap-10 px-5 py-20 lg:grid-cols-[0.8fr_1.2fr] lg:items-center lg:px-8"><div><p className="mb-3 text-sm font-bold uppercase tracking-[0.2em] text-primary">Our story</p><h2 className="font-serif text-4xl font-bold tracking-tight sm:text-5xl">Rooted in quality. Growing with Malawi.</h2></div><div className="max-w-2xl text-lg leading-8 text-secondary-foreground/75"><p>AgroFuturistics is a seedling nursery in Bunda, Lilongwe, built around one simple belief: every strong harvest starts with a strong beginning.</p><p className="mt-5">We raise quality tree and vegetable seedlings with patience and purpose, helping farmers and gardeners plant with confidence — and build stronger futures for their families and communities.</p></div></div></section>

      <section id="contact" className="mx-auto max-w-7xl px-5 py-20 lg:px-8"><div className="rounded-[2rem] bg-primary p-8 text-primary-foreground sm:p-12"><div className="flex flex-col justify-between gap-10 md:flex-row md:items-end"><div><p className="mb-3 text-sm font-bold uppercase tracking-[0.2em] text-accent">Visit or chat</p><h2 className="font-serif text-4xl font-bold">Tikupezeka ku Bunda, Lilongwe</h2><p className="mt-4 max-w-md leading-7 text-primary-foreground/70">We&apos;d love to help you choose what to plant next. Call or send us a WhatsApp message.</p></div><div className="flex flex-wrap gap-3"><a href={`tel:${DISPLAY_PHONE.replaceAll(' ', '')}`} className="rounded-full border border-primary-foreground/25 px-5 py-3 text-sm font-bold hover:bg-primary-foreground/10">{DISPLAY_PHONE}</a><a href={`https://wa.me/${WHATSAPP_NUMBER}`} target="_blank" rel="noreferrer" className="inline-flex items-center gap-2 rounded-full bg-accent px-5 py-3 text-sm font-bold text-accent-foreground"><MessageCircle size={17} /> WhatsApp us</a></div></div><div className="mt-10 flex items-center gap-2 border-t border-primary-foreground/15 pt-5 text-sm text-primary-foreground/60"><MapPin size={17} /> Bunda, Lilongwe, Malawi</div></div></section>
    </main>

    <footer className="border-t border-border"><div className="mx-auto flex max-w-7xl flex-col gap-3 px-5 py-8 text-sm text-muted-foreground sm:flex-row sm:items-center sm:justify-between lg:px-8"><p><span className="font-bold text-primary">AgroFuturistics</span> · Quality seedlings. Stronger futures.</p><p>© {new Date().getFullYear()} AgroFuturistics</p></div></footer>
    <a href={`https://wa.me/${WHATSAPP_NUMBER}`} target="_blank" rel="noreferrer" className="fixed bottom-5 right-5 z-30 grid size-14 place-items-center rounded-full bg-[#25D366] text-white shadow-xl transition hover:scale-105" aria-label="Chat with AgroFuturistics on WhatsApp"><MessageCircle size={25} /></a>

    {selected && <div className="fixed inset-0 z-50 grid place-items-center bg-foreground/50 p-4" role="dialog" aria-modal="true" aria-label={`${selected.name} details`}><div className="relative max-h-[90vh] w-full max-w-lg overflow-auto rounded-[2rem] bg-card shadow-2xl"><button onClick={() => setSelected(null)} className="absolute right-4 top-4 z-10 grid size-9 place-items-center rounded-full bg-card/90 text-foreground shadow" aria-label="Close details"><X size={18} /></button><img src={selected.image} alt={`${selected.name} seedlings`} className="h-64 w-full object-cover" /><div className="p-7"><p className="text-sm font-semibold text-primary">{selected.englishName}</p><h3 className="mt-1 font-serif text-3xl font-bold">{selected.name}</h3>{selected.variety && <p className="mt-2 text-sm text-muted-foreground">Variety: {selected.variety}</p>}<p className="mt-5 leading-7 text-muted-foreground">{selected.description}</p><div className="mt-7 flex items-center justify-between"><span className="font-serif text-2xl font-bold text-primary">{formatPrice(selected.price)} <small className="font-sans text-sm font-semibold text-muted-foreground">per seedling</small></span><button onClick={() => { updateCart(selected.id, 1); setSelected(null); setShowCart(true) }} className="rounded-full bg-primary px-5 py-3 font-bold text-primary-foreground">Add to order</button></div></div></div></div>}

    {showCart && <div className="fixed inset-0 z-50 bg-foreground/50"><aside className="ml-auto flex h-full w-full max-w-lg flex-col bg-card shadow-2xl"><div className="flex items-center justify-between border-b border-border p-5"><div><p className="text-sm font-semibold text-primary">Your order</p><h2 className="font-serif text-2xl font-bold">Mufuna kuyitanitsa?</h2></div><button onClick={() => setShowCart(false)} className="grid size-9 place-items-center rounded-full bg-secondary" aria-label="Close order"><X size={18} /></button></div><div className="flex-1 overflow-auto p-5">{!cartItems.length ? <div className="grid h-full place-items-center text-center"><ShoppingBasket className="mx-auto text-muted-foreground" size={40} /><p className="mt-4 font-bold">Your order is empty</p><p className="mt-1 text-sm text-muted-foreground">Choose some seedlings to get started.</p></div> : <><div className="space-y-3">{cartItems.map((item) => <div key={item.id} className="flex items-center justify-between gap-3 rounded-2xl bg-secondary p-3"><div><p className="font-bold">{item.name}</p><p className="text-sm text-muted-foreground">{formatPrice(item.price)} each</p></div><div className="flex items-center gap-2"><button onClick={() => updateCart(item.id, -1)} className="grid size-8 place-items-center rounded-full bg-card" aria-label={`Remove one ${item.name}`}><Minus size={15} /></button><span className="w-5 text-center text-sm font-bold">{cart[item.id]}</span><button onClick={() => updateCart(item.id, 1)} className="grid size-8 place-items-center rounded-full bg-primary text-primary-foreground" aria-label={`Add one ${item.name}`}><Plus size={15} /></button></div><p className="w-16 text-right font-bold">{formatPrice(item.price * cart[item.id])}</p></div>)}</div><div className="mt-8 border-t border-border pt-5"><div className="flex justify-between text-lg font-bold"><span>Total</span><span className="text-primary">{formatPrice(total)}</span></div><div className="mt-6 space-y-3"><label className="block text-sm font-bold">Your name<input value={form.name} onChange={(e) => setForm({ ...form, name: e.target.value })} className="mt-2 w-full rounded-xl border border-input bg-background px-4 py-3 font-normal outline-none focus:ring-2 focus:ring-ring" placeholder="e.g. Tamara Banda" /></label><label className="block text-sm font-bold">Phone / WhatsApp<input value={form.phone} onChange={(e) => setForm({ ...form, phone: e.target.value })} className="mt-2 w-full rounded-xl border border-input bg-background px-4 py-3 font-normal outline-none focus:ring-2 focus:ring-ring" placeholder="e.g. 0991 234 567" inputMode="tel" /></label><label className="block text-sm font-bold">How will you get your seedlings?<select value={form.method} onChange={(e) => setForm({ ...form, method: e.target.value })} className="mt-2 w-full rounded-xl border border-input bg-background px-4 py-3 font-normal outline-none focus:ring-2 focus:ring-ring"><option>Pickup from Bunda</option><option>Ask about delivery</option></select></label><button onClick={handleOrder} className="mt-3 flex w-full items-center justify-center gap-2 rounded-full bg-primary px-5 py-4 font-bold text-primary-foreground">Send order on WhatsApp <MessageCircle size={18} /></button>{submitted && <p className="flex items-center justify-center gap-2 text-center text-sm font-semibold text-primary"><Check size={15} /> Order prepared — WhatsApp is opening.</p>}</div></div></>}</div></aside></div>}
  </div>
}

function ProductCard({ product, quantity, onAdd, onMore }: { product: Product; quantity: number; onAdd: () => void; onMore: () => void }) {
  return <article className="group overflow-hidden rounded-[1.5rem] border border-border bg-card shadow-[0_8px_30px_rgba(27,94,32,0.06)] transition hover:-translate-y-1 hover:shadow-[0_14px_35px_rgba(27,94,32,0.12)]"><button onClick={onMore} className="block w-full text-left"><div className="relative aspect-[1.18] overflow-hidden bg-secondary"><img src={product.image} alt={`${product.name} (${product.englishName}) seedlings`} className="h-full w-full object-cover transition duration-500 group-hover:scale-105" loading="lazy" onError={(event) => { event.currentTarget.src = '/images/seedling-tray.png' }} />{product.status && <span className="absolute left-3 top-3 rounded-full bg-accent px-3 py-1 text-xs font-bold text-accent-foreground">Yakonzeka kubzalidwa</span>}</div><div className="p-5"><div className="flex items-start justify-between gap-3"><div><p className="text-sm font-semibold text-primary">{product.englishName}</p><h3 className="mt-0.5 font-serif text-2xl font-bold">{product.name}</h3>{product.variety && <p className="mt-1 text-sm text-muted-foreground">{product.variety}</p>}</div><span className="font-serif text-xl font-bold text-primary">{formatPrice(product.price)}</span></div></div></button><div className="flex items-center justify-between border-t border-border px-5 py-4"><button onClick={onMore} className="text-sm font-bold text-muted-foreground hover:text-primary">View details</button><button onClick={onAdd} className="inline-flex items-center gap-2 rounded-full bg-primary px-4 py-2 text-sm font-bold text-primary-foreground">{quantity ? <><span className="grid size-5 place-items-center rounded-full bg-primary-foreground/20">{quantity}</span> Added</> : <>Add to order <Plus size={15} /></>}</button></div></article>
}
